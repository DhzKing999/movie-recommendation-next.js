/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
        domains: [
            "picsum.photos",
            "m.media-amazon.com"
        ]
    }
};

export default nextConfig;
